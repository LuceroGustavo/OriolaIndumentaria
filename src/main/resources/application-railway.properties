# Railway Configuration - ORIOLA Indumentaria

# Server Configuration
server.port=${PORT:8080}

# Database Configuration (Railway MySQL)
spring.datasource.url=${MYSQL_URL}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Upload Configuration - Aumentado para videos
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=50MB

# File storage configuration para Railway (Volumen Persistente)
file.upload-dir=/app/data/uploads
backup.directory=/app/data/backups

# Static files configuration
spring.web.resources.static-locations=classpath:/static/,file:/app/data/uploads/
upload.path=/app/data/uploads
upload.thumbnail.path=/app/data/uploads/thumbnails

# Security Configuration
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin123}
spring.security.user.roles=ADMIN

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:luceroprograma@gmail.com}
spring.mail.password=${MAIL_PASSWORD:kmqh ktkl lhyj gwlf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Custom email configuration
app.email.from=${MAIL_USERNAME:luceroprograma@gmail.com}
app.email.to=${MAIL_USERNAME:luceroprograma@gmail.com}

# Logging configuration para Railway
logging.level.com.orioladenim=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
logging.level.org.springframework.security=WARN

# Configuración específica para videos de historias
app.historia.video.max-size=10MB
app.historia.video.max-duration=15
app.historia.video.allowed-formats=mp4,webm,mov,avi
app.historia.video.thumbnail-quality=0.8